# Django Girls

A React website for Django Girls workshops, schedules, and community information.

## Run locally

```bash
npm install
npm start
```

Open `http://localhost:3000`.

## Build

```bash
npm run build
```

## Deploy

Use Deploy.tz's **Static Website** stack. The `shipfile` builds and serves the app.
